# eocc

```text
eocc/
└── src/
    └── engine/
        ├── base/        // математика, логгер, память, время
        ├── system/      // окно, ввод, файлы, ОС-специфичное
        ├── assets/      // загрузчики шейдеров, glTF и пр.
        ├── renderer/    // OpenGL (и позже Vulkan)
        └── scene/       // сцена, объекты, компоненты
```